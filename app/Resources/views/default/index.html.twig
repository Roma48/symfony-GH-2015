{% extends 'base.html.twig' %}

{% block body %}
    <div id="wrapper">
        <div id="container" class="container">
            <div id="welcome">
                <h1>Commands</h1>
            </div>

            <table class="table table-striped table-hover table-responsive">
                <thead>
                <th>#</th>
                <th>Name</th>
                <th>Country</th>
                </thead>
                <tbody>

                {% for command in commands %}
                    <tr>
                        <td>{{ command.id }}</td>
                        <td><a href="{{ path('Command', {id: command.id}) }}">{{ command.name }}</a></td>
                        <td>{{ command.country }}</td>
                    </tr>
                {% endfor %}

                </tbody>
            </table>

        </div>
    </div>
{% endblock %}


